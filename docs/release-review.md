# 去敏感化与验证记录

检查日期：2026-09-22。此仓库是从程序文件白名单建立的新历史，不继承私人项目的 Git 历史或数据目录。GitHub 初始可见性为私有。

## 移除与替换

- 不复制教材、读物、提取正文、图片、音频、Anki 媒体/快照、课堂、作答、记忆、日志、备份及 CLI 登录状态。
- 清除源码中写死的历史成绩、日期词单、个人目标和固定桌面应用路径。
- 重写依赖私人记录的教材生成入口，使用原创演示或用户显式指定的材料包；保留特定版式导入适配器，明确其限制。
- 提供无凭据的示例配置，默认关闭 Anki 与 Codex。用户自行启用本机连接，Codex 路径通过 PATH 或配置确定。
- 保留每日词单、任务、课次证据、注音边界、后台恢复和备份；配套技能同步为通用项目定位与维护方式。

## 已完成的检查

1. 明确选取源码文件，不复制整个 dist 或 study；检查 Git 实际纳入清单及忽略规则。
2. 自带隐私检查脚本：扫描工作文件、暂存 blob 和可达 Git 历史 blob，检查凭据格式、私钥、邮箱、个人目录、受排除文件及二进制内容。扫描只输出类型与位置，不打印命中值。
3. 私人库标识交叉核对：以本机身份标识和真实课堂/Anki ID 形成2399条比对项，导出源码未命中。比对清单和原始值不随仓库发布。
4. Gitleaks 8.30.1 独立目录扫描未发现密钥；执行文件来自官方发布，归档 SHA-256 与发布校验和一致。
5. 仅使用 Git 纳入文件创建临时副本，重新生成示例数据；Python解析、JSON解析、全部前端JS语法检查通过。
6. 9组Python检查通过：原创材料导入、Anki镜像、日历、原生复习代理、课堂、记忆、补充题、每日流程/备份及每日备课。3组JS回归通过：词条合并、复习轮询交互、注音边界。
7. 隔离HTTP检查通过：首页/静态资源、健康、课堂、进度、每日安排/任务、服务状态、Anki离线端点；外部Host被拒绝。
8. 浏览器核对首页、零课堂/零作答/零记忆的记录页、演示书架与原创课文。发现并修正空历史JSON类型不匹配问题。
9. 两份配套技能格式校验通过。

提交后继续运行历史密钥扫描；上传后从私有远端重新克隆、重新验证。最终执行结果以交付时报告为准，不能将本文件当作未经执行的自动证明。

## 验证限制

这些检查不等于完整漏洞审计或绝对无泄漏保证。未调用真实付费 Codex 推理、未评分真实 Anki 卡片；新导出未使用私人书籍进行全量导入验证，未重新试听实际TTS。macOS之外的平台尚未验证。牌组范围和香港日期规则仍为当前约定，README 已明确。

准备公开前也应选择合适的软件许可；本仓库未擅自添加第三方材料或授予其再分发权限。
